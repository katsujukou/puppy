{
  "name": "puppy-docs",
  "type": "module",
  "scripts": {
    "compile": "cd .. && spago build -p puppy-docs",
    "test": "cd .. && spago test -p puppy-docs",
    "format": "purs-tidy format-in-place src test",
    "check": "purs-tidy check src test",
    "dev": "node scripts/render-docs.mjs && spago build && vite",
    "build": "spago build -p puppy-docs && vite build"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.0",
    "tailwindcss": "^4.3.0",
    "vite": "^8.0.16"
  },
  "dependencies": {
    "marked": "^18.0.5",
    "marked-gfm-heading-id": "^4.1.4",
    "minisearch": "^7.2.0",
    "shiki": "^4.2.0"
  }
}